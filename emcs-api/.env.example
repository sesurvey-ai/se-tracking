# emcs-api config

PORT=5500
LOG_LEVEL=INFO

# API key for write endpoints (POST/DELETE). Read endpoints (GET) also gated.
# Leave empty to disable auth (NOT recommended in production).
EMCS_API_KEY=

# SQLite path. In Docker, mount /data as a persistent volume.
EMCS_DB_PATH=data/emcs.db
